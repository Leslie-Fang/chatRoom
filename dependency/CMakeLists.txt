SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)
INCLUDE_DIRECTORIES(./gtest/include ./gtest)
SET(LIBGTEST ./gtest/src/gtest-all.cc)
ADD_LIBRARY(gtest STATIC ${LIBGTEST})
#INSTALL(TARGETS gtest 
#	ARCHIVE DESTINATION lib)